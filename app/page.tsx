'use client'

import { useMemo, useState } from 'react'
import { Activity, ArrowRight, BarChart3, Bell, Check, ChevronDown, ClipboardCheck, FileSpreadsheet, Filter, LayoutDashboard, Menu, MoreHorizontal, RotateCcw, Search, Settings, ShieldCheck, SlidersHorizontal, Stethoscope, Upload, UserRound, Users, X } from 'lucide-react'
import { Button } from '@/components/ui/button'

type View = 'home' | 'examiner' | 'results' | 'admin'
type Score = 'pass' | 'borderline' | 'fail' | null

const candidates = [
  { name: 'Amara Okafor', id: 'MED-2024-0087', station: 'Cardiology', status: 'In progress', initials: 'AO' },
  { name: 'Daniel Chen', id: 'MED-2024-0091', station: 'Respiratory', status: 'Completed', initials: 'DC' },
  { name: 'Sofia Martinez', id: 'MED-2024-0102', station: 'Neurology', status: 'Not started', initials: 'SM' },
  { name: 'James Wilson', id: 'MED-2024-0108', station: 'Abdominal', status: 'Completed', initials: 'JW' },
]

const criteria = ['Introduces self and confirms patient identity', 'Explores presenting complaint using open questions', 'Performs focused cardiovascular examination', 'Explains findings and offers a clear plan']

export default function Page() {
  const [view, setView] = useState<View>('home')
  const [mobileNav, setMobileNav] = useState(false)
  const [selectedCandidate, setSelectedCandidate] = useState(0)
  const [scores, setScores] = useState<Score[]>([null, null, null, null])
  const [toast, setToast] = useState('')
  const [search, setSearch] = useState('')
  const [weights, setWeights] = useState([25, 25, 25, 25])
  const [showPin, setShowPin] = useState(false)

  const filteredCandidates = useMemo(() => candidates.filter((candidate) => `${candidate.name} ${candidate.id}`.toLowerCase().includes(search.toLowerCase())), [search])
  const total = scores.every(Boolean) ? Math.round(scores.reduce((sum, score) => sum + (score === 'pass' ? 25 : score === 'borderline' ? 15 : 5), 0)) : 0

  const notify = (message: string) => { setToast(message); window.setTimeout(() => setToast(''), 2400) }
  const navigate = (next: View) => { setView(next); setMobileNav(false) }

  return (
    <div className="min-h-screen bg-background text-foreground">
      {view !== 'home' && <AppHeader view={view} navigate={navigate} mobileNav={mobileNav} setMobileNav={setMobileNav} />}
      {view === 'home' && <Landing navigate={navigate} />}
      {view === 'examiner' && <Examiner candidates={filteredCandidates} selectedCandidate={selectedCandidate} setSelectedCandidate={setSelectedCandidate} search={search} setSearch={setSearch} scores={scores} setScores={setScores} total={total} onSave={() => notify('Draft saved locally')} onSubmit={() => setShowPin(true)} />}
      {view === 'results' && <Results navigate={navigate} notify={notify} />}
      {view === 'admin' && <Admin search={search} setSearch={setSearch} weights={weights} setWeights={setWeights} notify={notify} />}
      {showPin && <PinModal close={() => setShowPin(false)} submit={() => { setShowPin(false); notify('Assessment submitted successfully') }} />}
      {toast && <div role="status" className="fixed bottom-6 left-1/2 z-50 -translate-x-1/2 rounded-lg bg-primary px-4 py-3 text-sm font-medium text-primary-foreground shadow-lg">{toast}</div>}
    </div>
  )
}

function AppHeader({ view, navigate, mobileNav, setMobileNav }: { view: View, navigate: (v: View) => void, mobileNav: boolean, setMobileNav: (v: boolean) => void }) {
  return <header className="border-b border-border bg-card"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 lg:px-8"><button onClick={() => navigate('home')} className="flex items-center gap-3"><span className="flex size-9 items-center justify-center rounded-xl bg-primary text-primary-foreground"><Activity className="size-5" /></span><span className="text-lg font-semibold tracking-tight">OSCE <span className="text-primary">Flow</span></span></button><nav className={`${mobileNav ? 'flex' : 'hidden'} absolute left-0 right-0 top-16 z-20 flex-col gap-1 border-b border-border bg-card p-4 lg:static lg:flex lg:flex-row lg:border-0 lg:bg-transparent lg:p-0`}><NavItem icon={LayoutDashboard} label="Overview" active={view === 'home'} onClick={() => navigate('home')} /><NavItem icon={ClipboardCheck} label="Examiner Station" active={view === 'examiner'} onClick={() => navigate('examiner')} /><NavItem icon={BarChart3} label="Student Results" active={view === 'results'} onClick={() => navigate('results')} /><NavItem icon={Settings} label="Admin" active={view === 'admin'} onClick={() => navigate('admin')} /></nav><div className="flex items-center gap-2"><button aria-label="Notifications" className="hidden rounded-lg p-2 text-muted-foreground hover:bg-muted sm:block"><Bell className="size-5" /></button><div className="hidden items-center gap-2 border-l border-border pl-3 sm:flex"><span className="flex size-8 items-center justify-center rounded-full bg-secondary text-xs font-semibold">JD</span><span className="text-sm font-medium">Dr. Jordan Davis</span><ChevronDown className="size-4 text-muted-foreground" /></div><button aria-label="Toggle navigation" className="rounded-lg p-2 lg:hidden" onClick={() => setMobileNav(!mobileNav)}><Menu className="size-5" /></button></div></div></header>
}
function NavItem({ icon: Icon, label, active, onClick }: { icon: typeof Activity, label: string, active: boolean, onClick: () => void }) { return <button onClick={onClick} className={`flex items-center gap-2 rounded-lg px-3 py-2 text-sm font-medium transition-colors ${active ? 'bg-primary/10 text-primary' : 'text-muted-foreground hover:bg-muted hover:text-foreground'}`}><Icon className="size-4" />{label}</button> }

function Landing({ navigate }: { navigate: (v: View) => void }) { return <main><section className="relative overflow-hidden bg-primary px-5 py-20 text-primary-foreground lg:px-8 lg:py-28"><div className="mx-auto grid max-w-7xl items-center gap-14 lg:grid-cols-[1.05fr_.95fr]"><div><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-primary-foreground/20 bg-primary-foreground/10 px-3 py-1.5 text-xs font-medium"><ShieldCheck className="size-3.5" />Built for modern medical education</div><h1 className="max-w-2xl text-balance text-5xl font-semibold tracking-tight sm:text-6xl">Assess with confidence. <span className="text-primary-foreground/60">Learn with clarity.</span></h1><p className="mt-6 max-w-xl text-pretty text-lg leading-8 text-primary-foreground/75">A calm, connected workspace for OSCE examiners, students, and administrators. Make every assessment fair, fast, and useful.</p><div className="mt-9 flex flex-wrap gap-3"><Button size="lg" variant="secondary" onClick={() => navigate('examiner')}>Open examiner station <ArrowRight data-icon="inline-end" /></Button><Button size="lg" variant="outline" className="border-primary-foreground/30 bg-transparent text-primary-foreground hover:bg-primary-foreground/10" onClick={() => navigate('results')}>View student results</Button></div></div><div className="rounded-2xl border border-primary-foreground/15 bg-primary-foreground/10 p-4 shadow-2xl"><div className="rounded-xl bg-card p-5 text-card-foreground"><div className="flex items-center justify-between border-b border-border pb-4"><div><p className="text-xs font-medium uppercase tracking-widest text-muted-foreground">Live assessment</p><h2 className="mt-1 text-lg font-semibold">Station 04 · Cardiology</h2></div><span className="flex items-center gap-1.5 rounded-full bg-accent px-2.5 py-1 text-xs font-medium text-accent-foreground"><span className="size-1.5 rounded-full bg-primary" /> In progress</span></div><div className="flex items-center gap-3 py-5"><span className="flex size-11 items-center justify-center rounded-full bg-primary/10 text-sm font-semibold text-primary">AO</span><div><p className="font-semibold">Amara Okafor</p><p className="text-sm text-muted-foreground">MED-2024-0087 · 06:42 remaining</p></div></div><div className="flex flex-col gap-3">{['Communication & rapport', 'Clinical examination', 'Clinical reasoning'].map((item, i) => <div key={item} className="flex items-center justify-between rounded-lg border border-border p-3"><span className="text-sm">{item}</span><span className={`rounded-md px-2 py-1 text-xs font-semibold ${i === 0 ? 'bg-accent text-accent-foreground' : 'bg-muted text-muted-foreground'}`}>{i === 0 ? 'Pass' : 'Pending'}</span></div>)}</div><div className="mt-5 flex items-center justify-between border-t border-border pt-4"><span className="text-xs text-muted-foreground">Auto-saved just now</span><span className="text-sm font-semibold text-primary">25 / 100</span></div></div></div></div></section><section className="border-b border-border bg-card px-5 py-10 lg:px-8"><div className="mx-auto flex max-w-7xl flex-wrap items-center justify-between gap-6"><p className="text-sm font-medium text-muted-foreground">One workspace for every role in your assessment program</p><div className="flex flex-wrap gap-x-8 gap-y-3 text-sm font-semibold text-muted-foreground"><span className="flex items-center gap-2"><Stethoscope className="size-4" />Clinical teams</span><span className="flex items-center gap-2"><Users className="size-4" />Universities</span><span className="flex items-center gap-2"><BarChart3 className="size-4" />Assessment leads</span></div></div></section><section className="mx-auto grid max-w-7xl gap-5 px-5 py-16 sm:grid-cols-3 lg:px-8"><Feature icon={ClipboardCheck} title="Structured scoring" text="Keep every station consistent with configurable rubrics and weighted criteria." /><Feature icon={BarChart3} title="Useful feedback" text="Turn assessment data into clear, actionable learning moments for every student." /><Feature icon={ShieldCheck} title="Trusted records" text="A clear audit trail and role-based views keep your assessment process secure." /></section></main> }
function Feature({ icon: Icon, title, text }: { icon: typeof Activity, title: string, text: string }) { return <div className="rounded-xl border border-border bg-card p-6"><span className="mb-5 flex size-10 items-center justify-center rounded-lg bg-accent text-accent-foreground"><Icon className="size-5" /></span><h3 className="font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{text}</p></div> }

function Examiner({ candidates: filtered, selectedCandidate, setSelectedCandidate, search, setSearch, scores, setScores, total, onSave, onSubmit }: any) { const candidate = filtered[selectedCandidate] || candidates[0]; return <main className="mx-auto max-w-7xl px-5 py-8 lg:px-8"><PageIntro eyebrow="Examiner station" title="Station 04 · Cardiology" description="Assess candidate performance against the station rubric." action={<Button variant="outline" onClick={onSave}><Check data-icon="inline-start" />Save draft</Button>} /><div className="mt-8 grid gap-6 lg:grid-cols-[280px_1fr]"><aside className="rounded-xl border border-border bg-card p-4"><div className="mb-4 flex items-center justify-between"><h2 className="font-semibold">Candidates <span className="ml-1 text-xs font-normal text-muted-foreground">({filtered.length})</span></h2><button className="text-muted-foreground"><MoreHorizontal className="size-5" /></button></div><div className="relative mb-4"><Search className="absolute left-3 top-2.5 size-4 text-muted-foreground" /><input aria-label="Search candidates" value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search name or ID" className="h-9 w-full rounded-md border border-input bg-background pl-9 pr-3 text-sm outline-none focus:ring-2 focus:ring-ring" /></div><div className="flex flex-col gap-1">{filtered.map((item: any, i: number) => <button key={item.id} onClick={() => setSelectedCandidate(i)} className={`flex items-center gap-3 rounded-lg p-3 text-left ${i === selectedCandidate ? 'bg-primary/10' : 'hover:bg-muted'}`}><span className="flex size-9 shrink-0 items-center justify-center rounded-full bg-secondary text-xs font-semibold">{item.initials}</span><span className="min-w-0"><span className="block truncate text-sm font-medium">{item.name}</span><span className="block truncate text-xs text-muted-foreground">{item.id}</span></span><span className={`ml-auto size-2 shrink-0 rounded-full ${item.status === 'Completed' ? 'bg-primary' : item.status === 'In progress' ? 'bg-chart-3' : 'bg-border'}`} /></button>)}</div></aside><section className="rounded-xl border border-border bg-card"><div className="flex flex-wrap items-center justify-between gap-4 border-b border-border p-5"><div className="flex items-center gap-3"><span className="flex size-12 items-center justify-center rounded-full bg-primary/10 font-semibold text-primary">{candidate.initials}</span><div><h2 className="font-semibold">{candidate.name}</h2><p className="text-sm text-muted-foreground">{candidate.id} · {candidate.station}</p></div></div><div className="text-right"><p className="text-xs uppercase tracking-widest text-muted-foreground">Current score</p><p className="text-2xl font-semibold text-primary">{total}<span className="text-sm font-normal text-muted-foreground"> / 100</span></p></div></div><div className="p-5"><div className="mb-5 flex items-center justify-between"><div><h3 className="font-semibold">Clinical assessment rubric</h3><p className="mt-1 text-sm text-muted-foreground">Select the performance level that best reflects the candidate.</p></div><span className="rounded-full bg-accent px-3 py-1 text-xs font-medium text-accent-foreground">6:42 remaining</span></div><div className="flex flex-col gap-3">{criteria.map((criterion, i) => <div key={criterion} className="rounded-lg border border-border p-4"><div className="flex items-start gap-3"><span className="flex size-6 shrink-0 items-center justify-center rounded-full bg-muted text-xs font-semibold">{i + 1}</span><p className="pt-0.5 text-sm font-medium">{criterion}</p></div><div className="mt-4 grid grid-cols-3 gap-2">{(['pass', 'borderline', 'fail'] as Score[]).map((score) => <button key={score} onClick={() => { const next = [...scores]; next[i] = score; setScores(next) }} className={`rounded-md border px-2 py-2 text-xs font-medium capitalize transition-colors ${scores[i] === score ? score === 'pass' ? 'border-primary bg-accent text-accent-foreground' : score === 'borderline' ? 'border-chart-3 bg-muted text-foreground' : 'border-destructive bg-destructive/10 text-destructive' : 'border-border text-muted-foreground hover:bg-muted'}`}>{score}</button>)}</div></div>)}</div><div className="mt-6 flex justify-end gap-3"><Button variant="outline" onClick={() => setScores([null, null, null, null])}><RotateCcw data-icon="inline-start" />Reset</Button><Button disabled={!scores.every(Boolean)} onClick={onSubmit}>Submit assessment <ArrowRight data-icon="inline-end" /></Button></div></div></section></div></main> }

function PageIntro({ eyebrow, title, description, action }: any) { return <div className="flex flex-wrap items-end justify-between gap-5"><div><p className="text-xs font-semibold uppercase tracking-widest text-primary">{eyebrow}</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">{title}</h1><p className="mt-2 text-sm text-muted-foreground">{description}</p></div>{action}</div> }

function Results({ navigate, notify }: { navigate: (v: View) => void, notify: (m: string) => void }) { return <main className="mx-auto max-w-5xl px-5 py-8 lg:px-8"><PageIntro eyebrow="Student results" title="Your assessment results" description="Review your OSCE performance and identify your next learning focus." action={<Button variant="outline" onClick={() => notify('PDF export prepared')}><FileSpreadsheet data-icon="inline-start" />Export report</Button>} /><div className="mt-8 grid gap-5 md:grid-cols-[1.2fr_.8fr]"><div className="rounded-xl border border-border bg-card p-6"><div className="flex items-center gap-4"><span className="flex size-14 items-center justify-center rounded-full bg-primary text-lg font-semibold text-primary-foreground">AO</span><div><h2 className="text-xl font-semibold">Amara Okafor</h2><p className="text-sm text-muted-foreground">MED-2024-0087 · Spring OSCE 2025</p></div></div><div className="mt-8 flex items-end justify-between"><div><p className="text-sm text-muted-foreground">Overall performance</p><p className="mt-1 text-5xl font-semibold text-primary">78<span className="text-lg text-muted-foreground"> / 100</span></p></div><span className="rounded-full bg-accent px-3 py-1.5 text-sm font-semibold text-accent-foreground">Pass</span></div><div className="mt-6 h-2 overflow-hidden rounded-full bg-muted"><div className="h-full w-[78%] rounded-full bg-primary" /></div><div className="mt-8 grid gap-3 sm:grid-cols-2">{['Communication & rapport', 'Clinical examination', 'Clinical reasoning', 'Professionalism'].map((item, i) => <div key={item} className="flex items-center justify-between rounded-lg bg-muted/60 p-3"><span className="text-sm">{item}</span><span className="font-semibold">{[86, 74, 71, 89][i]}%</span></div>)}</div></div><div className="rounded-xl border border-border bg-primary p-6 text-primary-foreground"><p className="text-xs font-semibold uppercase tracking-widest text-primary-foreground/60">Feedback summary</p><h2 className="mt-3 text-xl font-semibold">You&apos;re building strong rapport.</h2><p className="mt-3 text-sm leading-6 text-primary-foreground/75">Your communication and professionalism stood out. Focus next on making your clinical reasoning more explicit when explaining your plan.</p><div className="mt-8 border-t border-primary-foreground/15 pt-5"><p className="text-xs text-primary-foreground/60">Assessed by</p><p className="mt-1 text-sm font-medium">Dr. Jordan Davis · 18 May 2025</p></div></div></div><div className="mt-5 flex justify-start"><Button variant="ghost" onClick={() => navigate('home')}>Back to overview <ArrowRight data-icon="inline-end" /></Button></div></main> }

function Admin({ search, setSearch, weights, setWeights, notify }: any) { const sum = weights.reduce((a: number, b: number) => a + Number(b), 0); return <main className="mx-auto max-w-7xl px-5 py-8 lg:px-8"><PageIntro eyebrow="Program administration" title="Assessment overview" description="Configure stations, review cohorts, and keep your assessment program moving." action={<Button onClick={() => notify('Import dialog opened')}><Upload data-icon="inline-start" />Import roster</Button>} /><div className="mt-8 grid gap-4 sm:grid-cols-3"><Stat label="Assessments completed" value="248" trend="+12.4%" /><Stat label="Average score" value="76.8%" trend="+3.2%" /><Stat label="Pass rate" value="84.2%" trend="+5.8%" /></div><div className="mt-6 grid gap-6 lg:grid-cols-[1.4fr_.6fr]"><section className="rounded-xl border border-border bg-card p-5"><div className="flex flex-wrap items-center justify-between gap-4"><div><h2 className="font-semibold">Candidate roster</h2><p className="mt-1 text-sm text-muted-foreground">Spring OSCE 2025 · 312 candidates</p></div><div className="flex gap-2"><div className="relative"><Search className="absolute left-3 top-2.5 size-4 text-muted-foreground" /><input aria-label="Search roster" value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search roster" className="h-9 rounded-md border border-input bg-background pl-9 pr-3 text-sm outline-none focus:ring-2 focus:ring-ring" /></div><Button variant="outline" size="icon" aria-label="Filter roster"><Filter /></Button></div></div><div className="mt-5 overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b border-border text-xs uppercase tracking-wider text-muted-foreground"><tr><th className="pb-3 font-medium">Candidate</th><th className="pb-3 font-medium">Station</th><th className="pb-3 font-medium">Score</th><th className="pb-3 font-medium">Status</th></tr></thead><tbody>{candidates.filter(c => c.name.toLowerCase().includes(search.toLowerCase())).map(c => <tr key={c.id} className="border-b border-border last:border-0"><td className="py-4"><div className="flex items-center gap-3"><span className="flex size-8 items-center justify-center rounded-full bg-secondary text-xs font-semibold">{c.initials}</span><span><span className="block font-medium">{c.name}</span><span className="block text-xs text-muted-foreground">{c.id}</span></span></div></td><td className="py-4 text-muted-foreground">{c.station}</td><td className="py-4 font-semibold">{c.status === 'Completed' ? '82%' : '—'}</td><td className="py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${c.status === 'Completed' ? 'bg-accent text-accent-foreground' : 'bg-muted text-muted-foreground'}`}>{c.status}</span></td></tr>)}</tbody></table></div></section><section className="rounded-xl border border-border bg-card p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Rubric weights</h2><p className="mt-1 text-sm text-muted-foreground">Station 04 · Cardiology</p></div><SlidersHorizontal className="size-5 text-muted-foreground" /></div><div className="mt-6 flex flex-col gap-4">{['Communication', 'Examination', 'Reasoning', 'Professionalism'].map((label, i) => <label key={label} className="flex items-center justify-between gap-4 text-sm"><span>{label}</span><span className="flex items-center gap-1"><input aria-label={`${label} weight`} type="number" min="0" max="100" value={weights[i]} onChange={(e) => { const next = [...weights]; next[i] = Number(e.target.value); setWeights(next) }} className="h-9 w-16 rounded-md border border-input bg-background px-2 text-right outline-none focus:ring-2 focus:ring-ring" />%</span></label>)}</div><div className={`mt-5 flex items-center justify-between border-t border-border pt-4 text-sm font-semibold ${sum === 100 ? 'text-primary' : 'text-destructive'}`}><span>Total weight</span><span>{sum}%</span></div><Button className="mt-5 w-full" disabled={sum !== 100} onClick={() => notify('Rubric weights saved')}>Save configuration</Button></section></div></main> }
function Stat({ label, value, trend }: { label: string, value: string, trend: string }) { return <div className="rounded-xl border border-border bg-card p-5"><p className="text-sm text-muted-foreground">{label}</p><div className="mt-2 flex items-end justify-between"><p className="text-3xl font-semibold tracking-tight">{value}</p><span className="text-xs font-semibold text-primary">{trend}</span></div></div> }
function PinModal({ close, submit }: { close: () => void, submit: () => void }) { return <div className="fixed inset-0 z-40 flex items-center justify-center bg-foreground/30 p-5"><div role="dialog" aria-modal="true" aria-labelledby="pin-title" className="w-full max-w-sm rounded-xl border border-border bg-card p-6 shadow-xl"><div className="flex items-start justify-between"><div><h2 id="pin-title" className="text-lg font-semibold">Confirm submission</h2><p className="mt-1 text-sm text-muted-foreground">Enter your examiner PIN to submit this assessment.</p></div><button aria-label="Close" onClick={close} className="rounded-md p-1 text-muted-foreground hover:bg-muted"><X className="size-5" /></button></div><input aria-label="Examiner PIN" type="password" placeholder="••••" className="mt-6 h-11 w-full rounded-md border border-input bg-background px-3 text-center text-lg tracking-[0.5em] outline-none focus:ring-2 focus:ring-ring" /><div className="mt-5 flex gap-3"><Button variant="outline" className="flex-1" onClick={close}>Cancel</Button><Button className="flex-1" onClick={submit}>Submit</Button></div></div></div> }
